{
  "$schema": "../gen/schemas/desktop.json",
  "identifier": "default",
  "description": "Default capabilities for orchestrator-tool desktop",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
